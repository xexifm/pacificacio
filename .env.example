# Base path for GitHub Pages project hosting (https://xexifm.github.io/pacificacio).
# Leave empty when serving from a domain root (custom domain / user page).
# This is a build-time value; the deploy workflow sets it automatically.
NEXT_PUBLIC_BASE_PATH=/pacificacio
